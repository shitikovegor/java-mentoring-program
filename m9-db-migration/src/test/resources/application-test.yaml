spring:
  liquibase:
    enabled: false
  flyway:
    enabled: false
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/migration-flyway
    username: postgres
    password: dbPassword

